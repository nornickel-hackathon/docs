# P1 · task-011 · Snapshot + hash + персист  (Роль 1)
**Сделать:** заморозка графа в snapshot, детерминированный hash, запись в sqlite (см. docs/DATABASE.md); `/ingest` строит граф из сайдкара.
**Done:** один и тот же корпус → один и тот же hash; golden-тест воспроизводимости.
